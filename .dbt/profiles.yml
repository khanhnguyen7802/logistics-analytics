logistics_analytics:
  outputs:
    dev:
      type: duckdb
      path: /workspace/logistics_tracking.duckdb
      threads: 2
      schema: raw

    prod:
      type: duckdb
      path: /workspace/logistics_tracking.duckdb
      threads: 4

  target: dev
